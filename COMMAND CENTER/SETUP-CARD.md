# Purnaa Cap Nesting — Setup Card

Everything you need is in this one folder, **COMMAND CENTER**. There are four
buttons:

- **INSTALL** — once, the first time on a computer.
- **START** — every day you use the app.
- **UPDATE** — only when Max tells you to (updates the program).
- **Retrieve New Styles from P Drive** — when there are new styles to pull in.

Pick your computer below.

---

## 🪟 On a Windows computer (Santosh)

Your files are named with **.bat**:
`install.bat`, `start.bat`, `update.bat`,
`Retrieve New Styles from P Drive.bat`

### First time only — INSTALL
1. Double-click **install.bat**.
2. If a blue "Windows protected your PC" box appears, click
   **More info → Run anyway**.
3. Let it run. It may say *"close this window and run INSTALL again."*
   That is normal — close the window, double-click **install.bat** once more.
4. When it says **INSTALL COMPLETE**, you're done.

### Every day — START
1. Double-click **start.bat**.
2. A black window opens and your web browser opens to the app.
3. **Leave the black window open** the whole time you use the app.
4. When you're finished, close the black window to stop the app.

The app lives at: **http://localhost:4173**

### Only when Max says — UPDATE
1. Make sure the app is **closed** (black window closed).
2. Double-click **update.bat**.
3. When it says **UPDATE COMPLETE**, start the app as usual.

### When there are new styles — RETRIEVE NEW STYLES
1. Make sure you are **connected to the office network / P drive**.
2. Double-click **Retrieve New Styles from P Drive.bat**.
3. It pulls every style found across all P-drive backups (newest copy of each) plus the fabric list.
   This changes styles only, never the program.

---

## 🍎 On a Mac

Your files are named with **.command**:
`install.command`, `start.command`, `update.command`,
`Retrieve New Styles from P Drive.command`

### First time only — INSTALL
1. Double-click **install.command**.
2. If macOS says it "cannot be opened," **right-click the file →
   Open → Open**. You only have to do that once per file.
3. If a pop-up asks to install developer tools, click **Install**,
   wait for it to finish, then double-click **install.command** again.
   It will **not** ask for your password.
4. When it says **INSTALL COMPLETE**, you're done.

### Every day — START
1. Double-click **start.command**.
2. A black window opens and your web browser opens to the app.
3. **Leave the black window open** the whole time you use the app.
4. When you're finished, close the black window to stop the app.

The app lives at: **http://localhost:4173**

### Only when Max says — UPDATE
1. Make sure the app is **closed** (black window closed).
2. Double-click **update.command**.
3. When it says **UPDATE COMPLETE**, start the app as usual.

### When there are new styles — RETRIEVE NEW STYLES
1. Make sure you are **connected to the office network / P drive**.
2. Double-click **Retrieve New Styles from P Drive.command**.
3. It pulls every style found across all P-drive backups (newest copy of each) plus the fabric list.
   This changes styles only, never the program.

---

## ⚠️ If anything goes wrong

1. Take a **photo** of the whole black window with your phone.
2. Send the photo to **Max**.
3. **Do not ask Ryan to fix it** — this is Max's job, not Ryan's.

That's it. Nothing here can break your computer. When in doubt: photo → Max.
